framework:
    secret:        notasecret
    test: ~
    router:        { resource: "%kernel.project_dir%/Tests/Kernel/config/routing_test.yml" }
    profiler:      { only_exceptions: false }
    session:
        storage_id: session.storage.filesystem

twig:
    debug:            '%kernel.debug%'
    strict_variables: false

google:
    analytics:
        trackers:
            default:
                name: MyJavaScriptCompatibleVariableNameWithNoSpaces
                accountId: xxxxxx
                domain: .example.com
                allowHash: false
                allowLinker: true
                trackPageLoadTime: false
    adwords:
        conversions:
            account_create:
                id:    111111
                label: accountCreateLabel
                value: 0
